#include <iostream>
#include <lobby.h>

using namespace std;
 

lobby :: lobby(string user_role, string user_id, string chat_id,  
	int user_count, int chat_count, bool receieve_invite, string role_id) 
// : user_role(ur), user_id (ui), chat_id(ci), chat_history (ch), chat_time{ct}
  
 
string lobby :: chat_history(string [] messages) //
{


}		
 
int lobby :: chat_time() //return type is set as int for representing the time in milliseconds 
{
  
}

void lobby :: create_new_chat(string chat_id) //unsure about return type
{


}		
 
bool lobby :: delete_users(string user_id) //return type set as 'bool' as a check 
{
  
}




